// bullswarm contrib provider: opencode2 — the generic OpenCode CLI pool.
// connector.json is the whole pool; there is no meter. It is also the
// template a reseller provider clones through `ctx.templates.opencode2`
// (see docs/reference/providers.md).

export const name = 'opencode2';
export const displayName = 'OpenCode';
