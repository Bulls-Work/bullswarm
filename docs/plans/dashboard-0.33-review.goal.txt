The 0.33.0 owner-review pass in /home/dev/Repo/bullswork/bullswarm-dashboard (branch dashboard-0.33, PR #36, version 0.33.0): close the eight readability gaps the owner found while using the dashboard on a phone against real data. Evidence of each gap is the review screenshot in docs/design/owner-review-2026-09-18/ (named per page). The product keeps real figures and the ≈/basis rules; nothing is invented. Rendering is checked on a COPY of the real home (cp -Rp ~/.bullswarm /tmp/bsw-review-home, then BULLSWARM_HOME=/tmp/bsw-review-home node bin/bullswarm.js workflow reindex), never on ~/.bullswarm itself, at 55x26 (phone portrait), 60x50 (tall phone), 170x35 (phone landscape) and 200x50, with tmux + scripts/tui-shot.py.

1. Phone Home ([screenshot removed: it showed private project data]) shows every budget pool the desktop shows (four today, no silent cap at three) or says how many it left out; pool names in that block are wide enough that claude-code:acme and claude-code read differently; the by-pool / by-model / by-project lists show at least their top three rows or say `+N more`, with the percentage always fully visible (never `24…`); the summary figures sit one per line so `Busiest project` and `Favourite model` are not cut; the spent tile's empty-state phrase fits the phone row.
2. `h` opens Home and `?` opens Help everywhere the key table, the nav row, the Help page, the README, docs/guide/observing.md and the CHANGELOG describe keys (the changelog line that says h/? are unchanged is corrected); `r b s y f` keep their pages; tests pin the new bindings.
3. A finished run that has no history index entry yet is named by the project derived from its goal.json working directory (the same derivation `workflow reindex` uses), never `unknown project` ([screenshot removed: it showed private project data]); `unknown project` remains only when no working directory is recorded.
4. The stacked column charts on Home (spent per day) and Stats Trends ([screenshot removed: it showed private project data]) are drawn to eighth-block precision (▁▂▃▄▅▆▇█ on the top cell), a bar never rises above its own axis line, the axis top and row count are chosen together so every tick label sits on a row with even spacing, and every non-zero pool slice is visible as at least one eighth-block (smallest slices on top) so codex and command-code appear whenever they spent anything.
5. Stats Pools ([screenshot removed: it showed private project data]) replaces the shared unlabelled "licence used per day, by pool" line chart with a seven-day sparkline on each pool's own row (one glyph per day of end-of-day usage, `▏` where a window reset) and one caption saying a drop after `▏` is the window resetting; the row tails (`≈ $3.06 API`, `✓ 100%`) are measured so nothing truncates at 170 or 200 columns.
6. Budget ([screenshot removed: it showed private project data]) reads in plain words: per pool a header `name · weekly plan · resets <day time> (in Nd Nh)`, then labelled rows `used` (meter, whole percent, `N% of the window gone → on track|slow`), `by bullswarm` (this share of the plan as a whole percent with the measured minutes, and `other tools N%`), `room` (`about N more medium runs before the reset`), `so far` (≈ money and the two biggest workflows with ≈ money); whole numbers only; the subscription line appears only when a price is declared; the `Subscription rate: —` lines and the `workflows 131.24% · rest 0% · workflows exceeds the reported meter` wording are gone, replaced by one footnote in words when the measured share exceeds the meter; the page-foot notes are one short block.
7. Runs and History are one page ([screenshot removed: it showed private project data] vs [screenshot removed: it showed private project data]): the Runs page shows an `active` block first (each running or waiting run with its plan strip and per-step bar exactly as Home's running section draws it) and then History's day-grouped table (day header with count and ≈ total, one row per run with status glyph, short id, project, goal, duration, ≈ cost, time) with the selection cursor, so Enter and the digits open a run and older days load on scroll; the `agents` and `run it` blocks move to the Help page; the History tab is removed from the tab row and `y` jumps to the first day header of Runs; the tab row is Home, Runs, Budget, Stats, Fleet.
8. No page draws an empty track for missing data ([screenshot removed: it showed private project data]): the Run page's budget block is titled `licence this run used`, shows whole percents (`≈ 0.4% of the weekly plan`), writes `free model · no licence meter` for an unmetered pool instead of a dotted track and a dash, and collapses to one sentence when no pool is metered; the plan strip's pool row shows only the pool name when the cell cannot fit pool and model; a paused run's live block says `paused · bullswarm workflow resume <id> continues it` instead of `waiting for the next dispatch`; the footnote `N of N remaining steps recorded no expected duration` becomes `ETA —: <step> has no recorded duration yet`; the same rule removes dotted `meter unavailable` tracks on Stats Pools and any other page in favour of one line of words.
