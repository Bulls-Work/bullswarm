Plan the visual-fidelity pass that makes the 0.33.0 dashboard match the approved prototype. 1. A page-by-page composition gap list comparing the real product's screens (rendered from the local build at 55, 120 and 200 columns) with the prototype frames in docs/design/prototype-frames and the screenshots in docs/design/prototype-shots. 2. A validated bullswarm workflow program (goal text plus program JSON under docs/plans/) whose requirements are per-page composition and colour fidelity and whose acceptance renders the product to PNG with scripts/tui-shot.py and compares it with the prototype.