Bullswarm 0.35.1 follow-up: the rebuilt Step and Run pages paint the approved colours, and four defects the QA pass found are fixed. The two design records are the acceptance target: docs/design/tidy-0.35.1/step-v2/README.md and docs/design/tidy-0.35.1/run-v2/README.md, each ending in "## Colour rules (approved 2026-09-21)". Real data only: every frame and fixture comes from the read-only snapshot home; unknown money stays a dash; no test.skip anywhere.

1. Colour. The Step page, the Run page and the single-task page paint exactly the "Colour rules" table of the records with the existing helpers (strong, dimText, tint with METER_COLORS roles, seriesColor for pools): green good verdicts, amber running marks, red failures, dim pending marks and meta (clocks, counts, labels, basis phrases, hints, rule dashes, money glyphs), bold identity and amounts, pool names in their series colour, plain response/task/report/command text, inverse cursor row. In ASCII mode (asciiGlyphsPreferred) only bold, dim and inverse remain. Tests pin the cells the records name, on real snapshot frames at 200 and 55.
2. The single-task page header names the task as the Runs list does: `<lane> task · <8-char id>` (for example `analyze task · 3ad03092`), never the full UUID or a 6-character prefix; the verdict line and the pool · model · effort row follow the Step header grammar.
3. Home cards print durations with the same h/m/s clock as the Step and Run pages (`5h55m active of 6h55m`, `58m37s`, span only when it differs from active), never decimal minutes like `active 725.95m`.
4. The Runs page opens with the cursor on its first row (the first active run when any is running) and that row is drawn inverse, so Enter opens the highlighted row; moving Up from the first row stays on it.
5. Frames: the real text frames under docs/design/tidy-0.35.1/frames/real-*.txt are regenerated, and a new directory docs/design/tidy-0.35.1/frames/colour/ holds the same Step (finished, running, detail), Run (running, finished), single-task and Home frames at 55 and 200 WITH their SGR codes kept (rendered from the snapshot by an option or sibling script of scripts/render-tidy-0.35.1-frames.mjs), so a reviewer can grep the codes; no frame line exceeds its width in display cells.
6. docs/guide/observing.md gains a short "Colours" paragraph naming the roles in words; CHANGELOG.md's ## 0.35.1 section gains bullets for the colours and the three fixes; npm test passes with 0 failures and 0 skipped (the three load-sensitive tests may be rerun alone).
