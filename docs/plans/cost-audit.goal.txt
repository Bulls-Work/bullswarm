Cost audit and step-timeline study for bullswarm, in /home/dev/Repo/bullswork/bullswarm-cost (branch cost-audit from v0.33.0). [owner request, paraphrased] The API-equivalent cost bullswarm records per attempt (attempt.usage.cost.estimatedUsd, shown as "API≈" on the dashboard) is suspected to understate real consumption heavily. Real data only: every token count and price must come from a transcript, a state file or a vendor page you loaded and quote; where a figure cannot be obtained, say so in words. Write only under docs/studies/cost-audit-2026-09-18/ and scripts/cost-audit/; never modify src/, tests/, ~/.bullswarm or any transcript; no commits, no push.

Requirements:
1. Estimator audit: a report tracing how attempt.usage is produced (src/lib/usage.js estimateInvocationUsage and parseReportedUsage, the provider connectors, src/lib/spend.js) and how it is aggregated into the API≈ figures on Home, Runs, Stats and Budget (src/workflow/rollup.js, stats-model.js, budget-model.js, history.js), naming every place a number is estimated from bytes rather than measured, and which providers ever yield provider-reported usage.
2. Claude actual vs recorded: for the claude-code:acme attempts fuyyca/integrate-2 (2026-09-17T20:39:58Z to 21:15:23Z, cwd /home/dev/Repo/bullswork/bullswarm-dashboard), ipccf2/accept-3 (18:21:12Z, 32 min) and ipccf2/accept-4 (19:24:15Z, 15 min; both cwd /home/dev/Repo/bullswork/project-a), locate the Claude Code transcript (~/.claude-acme/projects/<escaped cwd>/*.jsonl, matched by time window), sum the per-message usage (input, cache creation, cache read, output tokens; dedupe repeated message ids; include subagent sidechains), price it at the published Claude Opus rates with the URL and quoted lines, and tabulate recorded tokens and USD against actual tokens and USD with the ratio, using a reusable script in scripts/cost-audit/.
3. Grok and Codex actual vs recorded: the same for fuyyca/verify-1 (grok, grok-4.6, 21:15:23Z, 26 min; find where the grok CLI keeps transcripts or token counts under ~/.grok) and for two codex attempts of run 2yrcxi (integrate-1 gpt-5.6-sol 04:08:58Z 16 min, widget-1 gpt-5.6-luna 03:12:47Z 44 min; ~/.codex/sessions/<year>/<month>/<day>/*.jsonl token_count events), each priced from a vendor page you quote, or marked "price not found" when none exists.
4. Step timeline study: what project-n (/home/dev/project-n: packages/parser/src claude-code.ts, codex.ts, presentation.ts; apps/web/app/sessions/[id]/session-view.tsx and team-tab/turn-drawer.tsx) does to turn a transcript into a readable session (turn grouping, tool cards, subagent and parallel-run handling, mini-map timeline, per-turn tokens), what bullswarm's normalized stream already carries (read-only reference: /home/dev/Repo/bullswork/bullswarm-0.33.1/src/lib/agent-events.js and attempt-stream.js, and the events a stream-<action>-attempt-N.jsonl file holds), the gap between them, and a design for the bullswarm Step page: an attempt timeline with turns, tool calls, subagent groups and per-turn tokens, with two text frames at 55 and 120 columns, plus the additions the stream capture needs.
5. Cost fix plan: from requirements 1 to 3, a plan with file anchors to make the recorded usage measured rather than estimated: capture the session id and the provider's own totals (Claude `result` event fields, Codex token_count events, Grok equivalent) at attempt end, fall back to transcript lookup, back-fill past attempts from transcripts, and show the confidence basis on the dashboard; sized for a plan revise of the running 0.33.1 workflow or a 0.33.2 release, with the trade-offs in words.
6. Every file written lives under docs/studies/cost-audit-2026-09-18/ or scripts/cost-audit/, `git status` shows nothing else changed, and no file under ~/.bullswarm, ~/.claude-acme, ~/.codex or ~/.grok was modified.
