The 0.33.0 visual-fidelity pass in /Users/cowcow02/Repo/bullswork/bullswarm-dashboard: make the shipped dashboard match the approved prototype's composition, density and colour, page by page, at 55, 120 and 200 columns. Frames are docs/design/prototype-frames/<page>-<width>.txt with .tagged.txt colour twins; screenshots are docs/design/prototype-shots/; scripts/tui-shot.py renders the real product to PNG for the same comparison. The prototype's numbers are illustrative: the product keeps its real figures and its ≈/basis rules, and only the composition, density, colour and wording move.

1. Home matches the prototype frame's composition, density and colour at 55, 120 and 200 columns while keeping real data and the ≈/basis rules (frames home-55.txt and home-120.txt): the 7-day breakdown as four columns on one row band, a `── budget · this week ──` block, per-step bars in the running rows, the three-column summary band with its closing sentence, and one-row tiles on the phone.
2. Run and Step match the prototype frame's composition, density and colour at 55, 120 and 200 columns while keeping real data and the ≈/basis rules (frames run-55.txt, run-120.txt, step-55.txt and step-120.txt): Run flat and ruled with the plan DAG, per-step bars and the budget · live · so far triptych instead of box-drawing panels, and Step as a label/value table with task, output and artifacts sections.
3. Budget matches the prototype frame's composition, density and colour at 55, 120 and 200 columns while keeping real data and the ≈/basis rules (frames budget-55.txt and budget-120.txt): the percentage on the meter row over a textured meter with a ▏ elapsed mark, seven rows per pool, biggest workflows on one row, and one page footer instead of ten rows of phone footnotes.
4. Stats overview and trends match the prototype frame's composition, density and colour at 55, 120 and 200 columns while keeping real data and the ≈/basis rules (frames stats-overview-*.txt and stats-trends-*.txt): the heatmap sized to the history that exists with month labels and the four-shade ramp, a three-column figure grid carrying most active day, current streak, longest streak and longest run, and Trends' stacks coloured per pool with a drawn running-total row and a capped legend.
5. Stats pools, models and projects match the prototype frame's composition, density and colour at 55, 120 and 200 columns while keeping real data and the ≈/basis rules (frames stats-pools-*.txt, stats-models-*.txt and stats-projects-*.txt): a licence-per-day chart with severity colour and a reset mark instead of identical full-width rows, a model list keyed by colour to its chart, and a sparkline beside each project name.
6. History matches the prototype frame's composition, density and colour at 55, 120 and 200 columns while keeping real data and the ≈/basis rules (frames history-55.txt and history-120.txt): fixed columns with the ✓/✗ mark restored, one row per run on the phone, the day rule with its coloured total, and `(≈ $X API)` instead of the repeated long label.
7. Fleet and Help match the prototype frame's composition, density and colour at 55, 120 and 200 columns while keeping real data and the ≈/basis rules (frames fleet-55.txt, fleet-120.txt, help-55.txt and help-120.txt): Fleet's sub-tab row on one line with the edit hint beside it and cyan model names, and Help's keys grouped by purpose onto shared rows with bold key names and no claim about sub-tabs Budget does not have.
8. The shared palette and layout helpers: purple, orange, cyan and grey roles plus a four-shade heat ramp added to the one palette every page already draws from, and column and compact phone row helpers in the render kit, so no page invents its own colour or its own layout.
9. The phone layouts: every page renders one row per item at 55 columns (54 painted), the frame-width subtraction is unchanged, and no painted line exceeds the frame width at any width from 32 to 200 columns.
10. Tests under tests/ cover the new palette, column and compact-row atoms and every page at 55×26, 120×40 and 200×50 with no line past the width, and `npm test` passes.
11. docs/guide/observing.md and README.md describe the composition the pages now use, and CHANGELOG.md carries the visual-fidelity entry in the file's voice without bumping package.json.
