Bullswarm 0.33.1: fold the reviewed fixes, make the free pool safe, and close the dashboard and Mod gaps the owner found on 2026-09-18, in /Users/cowcow02/Repo/bullswork/bullswarm-0.33.1 (branch release-0.33.1 from v0.33.0). Real data only: never invent a figure, price or API fact; where a value cannot be measured or cited, say so in words. Nothing outward-facing: no push, no PR, no tag.

Requirements:
1. The four reviewed pull requests are folded onto the branch with their tests green: #37 attempt handoff (local ref pr-37), #35 a signalled kernel writes a terminal lifecycle (pr-35), #34 pnpm global installs upgrade via pnpm (pr-34), #33 dedupe Claude logins by account identity (pr-33). CHANGELOG bullets land under `## Unreleased`.
2. Free-model liveness probe: before dispatching to a pool whose picked rung names a free model, bullswarm sends a one-word prompt through that pool's own CLI (timeout ~30 s), caches the answer per model for ~15 minutes, benches the pool with the reason on failure (404, provider error, timeout) and falls through to metered pools; the reason appears in `routeWhy`, `bullswarm pools` and the Fleet page. It never probes a rung set to the CLI default, never a paid model, never scans a catalogue, and never picks a replacement model.
3. A worker that recovers from a provider stream error and finishes its task is not failed for the error: the attempt succeeds when the output is usable, and the recovered error is recorded on the attempt as a note.
4. Single tasks (`bullswarm run`) are visible: each dispatch's decision-log entry records the task file, output file and lane; in-flight tasks come from ~/.bullswarm/assignments/*.json (source "run"); the Runs page lists in-flight tasks in its active block and finished tasks as rows in the day table (glyph, lane, pool · model, ok or reason, duration, time), Home's today band counts them, and the Mod shows an in-flight task instead of "No ongoing workflow run".
5. Plan prices without typing: each provider reports the plan name it can detect (Claude subscriptionType/rateLimitTier from ~/.claude/.credentials.json, Codex plan_type, Command Code plan, opencode plan); data/plan-prices.json maps plan names to monthly USD ONLY where the worker loaded a vendor page and quotes its URL and the exact line; unknown stays null; Budget uses a detected price the same way it uses a declared one and says `detected` vs `declared`.
6. Budget verdict prototype frames (no code): docs/design/prototype-frames-0.33.1/budget-55.txt and budget-120.txt drawn from the real pool figures of 2026-09-18, one line per pool sorted by urgency in words, one bar per pool, details behind Enter described in the README.
7. Home "today" prototype frames (no code): home-today-55.txt and home-today-120.txt — left: card-style finished runs of today with what each was for and its project; right: today's licence spend per pool and the share depleted by workflows, single runs and minutes.
8. The Mod pane's `usage` button works with no run in flight (it renders the pools page), and a `run` assignment in flight is shown on the empty pane.
9. Budget shows every meter window a pool reports (5-hour, 7-day, monthly) with reset time and pace, and the header says how old the sample is (`sampled 5m ago`).
10. Run page polish: attempts record output bytes over time so the Run and Step pages draw a live output sparkline; the plan strip shows the pool name whole or not at all, never `openc…`; a retry after a kernel restart still receives the handoff block, built from the durable attempt record; stalled attempts are excluded from `medianMinutes` in strategy.js.
11. `npm test` passes on the final tree; docs/guide/observing.md, docs/reference/cli.md and the Mod README describe the shipped behaviour; CHANGELOG `## Unreleased` has one plain-words bullet per requirement; package.json is not bumped.
